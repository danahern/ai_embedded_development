---
paths: [".claude/commands/**", ".mcp.json"]
---
# Operational Learnings

- **MCP setup on new machine: build servers, symlink skills, restart** — When setting up the workspace on a new machine:
